export enum RoutePaths {
  // un-authorized
  entry = "/",
  signUp = "/auth/sign-up",
  signIn = "/auth/sign-in",

  // authorized
  home = "/home",
  search = "/search",
  messages = "/messages",
  composeFeed = "compose/feed",
}

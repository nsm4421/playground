import 'package:flutter/material.dart';

class PayFragment extends StatelessWidget {
  const PayFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

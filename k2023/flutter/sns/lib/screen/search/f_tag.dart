import 'package:flutter/material.dart';

class TagFragment extends StatelessWidget {
  const TagFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Tags"),
    );
  }
}

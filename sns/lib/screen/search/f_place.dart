import 'package:flutter/material.dart';

class PlaceFragment extends StatelessWidget {
  const PlaceFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Place"),
    );
  }
}

import 'package:flutter/material.dart';

class TopFragment extends StatelessWidget {
  const TopFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("TOP"),
    );
  }
}

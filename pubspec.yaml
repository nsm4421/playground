name: commerce_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  dart_code_metrics: ^5.7.6
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.7
  go_router: ^11.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/image/
    - assets/lottie/
    - assets/svg/

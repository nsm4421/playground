import 'package:flutter/material.dart';

class SearchFeedScreen extends StatelessWidget {
  const SearchFeedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Search Feed"),),
    );
  }
}

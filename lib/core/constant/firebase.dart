enum CollectionName {
  user("users"),
  short("shorts"),
  chat("chats"),
  message("messages");

  final String name;

  const CollectionName(this.name);
}

enum BucketName {
  user("users"),
  short("shorts"),
  chat("chats"),
  message("messages");

  final String name;

  const BucketName(this.name);
}

part of 'index.dart';

class EditCaptionFragment extends StatelessWidget {
  const EditCaptionFragment(this.asset, {super.key});

  final AssetEntity asset;

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child:
      Column(
        children: [

        ],
      ),
    );
  }
}

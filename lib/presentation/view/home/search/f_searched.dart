part of 'index.dart';

class SearchedFragment extends StatelessWidget {
  const SearchedFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

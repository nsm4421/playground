part of 'index.page.dart';

class DisplayDiariesScreen extends StatelessWidget {
  const DisplayDiariesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("TEST"),
      ),
    );
  }
}
